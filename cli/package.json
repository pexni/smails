{
	"name": "@smails/cli",
	"version": "0.0.2",
	"description": "Temporary email for humans and agents — CLI + MCP server",
	"type": "module",
	"files": [
		"dist"
	],
	"bin": {
		"smails": "dist/index.js"
	},
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "tsup src/index.ts --format esm --shims",
		"dev": "tsup src/index.ts --format esm --shims --watch"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.1",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"tsup": "^8.0.0",
		"typescript": "^5.5.0"
	}
}
